document.getElementById('capture').addEventListener('click', async () => {
  // Placeholder: trigger screenshot flow via messaging to content script/background
  alert('Capture triggered (stub)');
});
